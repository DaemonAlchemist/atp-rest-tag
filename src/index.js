
import tagRoutes from './routes/tag';

//import validators from "./validators/index";

export default ({
    routes: {
        tag: tagRoutes,
    },
    config: {
        //validators
    }
});
